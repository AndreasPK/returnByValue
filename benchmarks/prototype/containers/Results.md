Just a quick & dirty benchmark to show this is worth it.

For sequence-benchmarks the benchmarks improved in the geometric mean by 1.93%
with set-benchmarks-set showing similar speedups but I did not yet a full run of these.

